3.8 创建和运行程序
  注意component的顺序，参数的设置
 
  3.8.1 在Datastore上运行程序
    Datastore可以避免同时加载大量文件，在datastore上运行程序的时候，每个文件会被加载、处理、存储然后卸载。这样可以避免内存的不足但是运行速度较慢。需要注意的时候，如果来自datastore corpus的文件在运行程序之前已经被加载的话，那它既不会被卸载也不会被存储。想要存储这样的文件，在resources tree中右击它然后将其存储到datastore。

  
